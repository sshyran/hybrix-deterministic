import { long, TransferTransaction } from '../transactions';
import { SeedTypes, Params } from '../types';
export interface TransferParams extends Params {
    recipient: string;
    amount: long;
    attachment?: string;
    feeAssetId?: string;
    assetId?: string;
    fee?: long;
    timestamp?: number;
}
export declare const transferToBytes: (tx: TransferTransaction) => Uint8Array;
export declare const transferValidation: (tx: TransferTransaction) => never[];
/**
 * Creates and signs [[TransferTransaction]].
 *
 * You can use this function with multiple seeds. In this case it will sign transaction accordingly and will add one proof per seed.
 * Also you can use already signed [[TransferTransaction]] as a second argument.
 *
 * ### Usage
 * ```js
 * const { transfer } = require('waves-transactions')
 *
 * const seed = 'b716885e9ba64442b4f1263c8e2d8671e98b800c60ec4dc2a27c83e5f9002b18'
 *
 * //Transfering 1 WAVES
 * const params = {
 *   amount: 100000000,
 *   recipient: '3P23fi1qfVw6RVDn4CH2a5nNouEtWNQ4THs',
 *   //feeAssetId: undefined
 *   //assetId: undefined
 *   //attachment: undefined
 *   //timestamp: Date.now(),
 *   //fee: 100000,
 * }
 *
 * const signedTransferTx = transfer(params, seed)
 * ```
 * ### Output
 * ```json
 * {
 *   "id": "8oH5RKNJzqbAWVbqqcb9nwDoCb3osZiouLXJMzj65n6g",
 *   "type": 4,
 *   "version": 2,
 *   "fee": 100000,
 *   "senderPublicKey": "G62H1XE5rnaCgCCURV5pWwQHzWezZB7VkkVgqthdKgkj",
 *   "timestamp": 1542716926744,
 *   "proofs": [
 *     "MxK7z1Lm9EYqrWwDLJ6hEtqc3dXt2XUgMrd9pBoF5zJGWDBaYboe1EcncfQZxx18WrxkT4pA4QMhBTvbFT32eB1"
 *   ],
 *   "amount": 100000000,
 *   "recipient": "3P23fi1qfVw6RVDn4CH2a5nNouEtWNQ4THs"
 * }
 * ```
 *
 * @param seed
 * @param paramsOrTx
 * @returns
 *
 */
export declare function transfer(paramsOrTx: TransferParams | TransferTransaction, seed?: SeedTypes): TransferTransaction;
