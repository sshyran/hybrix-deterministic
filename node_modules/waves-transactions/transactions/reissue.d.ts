import { ReissueTransaction, long } from '../transactions';
import { SeedTypes, Params } from '../types';
import { ValidationResult } from 'waves-crypto/validation';
export interface ReissueParams extends Params {
    assetId: string;
    quantity: long;
    reissuable: boolean;
    fee?: long;
    timestamp?: number;
    chainId?: string;
}
export declare const reissueValidation: (tx: ReissueTransaction) => ValidationResult;
export declare const reissueToBytes: (tx: ReissueTransaction) => Uint8Array;
/**
 * Creates and signs [[ReissueTransaction]].
 *
 * You can use this function with multiple seeds. In this case it will sign transaction accordingly and will add one proof per seed.
 * Also you can use already signed [[ReissueTransaction]] as a second argument.
 *
 * ### Usage
 * ```js
 * const { reissue } = require('waves-transactions')
 *
 * const seed = 'b716885e9ba64442b4f1263c8e2d8671e98b800c60ec4dc2a27c83e5f9002b18'
 *
 * const params = {
 *   quantity: 10000,
 *   assetId: '3toqCSpAHShatE75UFKxqymuWFr8nxuxD7UcLjdxVFLx',
 *   reissuable: false,
 *   //timestamp: Date.now(),
 *   //fee: 100000000,
 *   //chainId: 'W'
 * }
 *
 * const signedReissueTx = reissue(params, seed)
 * ```
 * ### Output
 * ```json
 * {
 *   "id": "7HR6PGb2eP1bDs1AQ3fVXrNAKU6Ay57pwUPLW2aLUBP4",
 *   "type": 5,
 *   "version": 2,
 *   "chainId": "W",
 *   "fee": 100000000,
 *   "senderPublicKey": "G62H1XE5rnaCgCCURV5pWwQHzWezZB7VkkVgqthdKgkj",
 *   "timestamp": 1542716926734,
 *   "proofs": [
 *     "8kuyzXRnMyVxoCwDZr5MspPq89YBJEnUgZ3NFSMqA2mUQP6cseXy6ncJoRDVfNw8vhuen8u5z8popPLQXbhA31U"
 *   ],
 *   "quantity": 10000,
 *   "assetId": "3toqCSpAHShatE75UFKxqymuWFr8nxuxD7UcLjdxVFLx",
 *   "reissuable": false
 * }
 * ```
 *
 * @param seed
 * @param paramsOrTx
 * @returns
 *
 */
export declare function reissue(paramsOrTx: ReissueParams | ReissueTransaction, seed?: SeedTypes): ReissueTransaction;
