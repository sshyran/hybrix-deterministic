global.TronWeb = require('../dist/TronWeb.node.js');

const {TQ_API, SHASTA_API, PRIVATE_KEY} = require('./config')

const API = 'http://v2.api.staging.trongrid.io'

global.tronWeb =  new TronWeb(
    API,
    API,
    API,
    PRIVATE_KEY
    )

require('repl').start({});


async function get(){
    let res
    try {
        res = await tronWeb.getEventResult("TBg5T7ufLSmTdPZ6dXeX9hU1inUax6ZFs2", 0, "Notify");
    } catch(err) {
        console.error(err)
    }
    console.log(res);
    return res;
}

get();
